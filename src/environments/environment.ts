/**
 * Configuración de entorno (producción / default).
 * `apiUrl` apunta al backend Spring Boot. Cambiá este valor para apuntar
 * a tu backend desplegado. En desarrollo se usa environment.development.ts.
 */
export const environment = {
  production: true,
  apiUrl: '',
};
