/**
 * Configuración de entorno para desarrollo (`ng serve`).
 * Apunta al backend corriendo localmente en el puerto 8080.
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};
